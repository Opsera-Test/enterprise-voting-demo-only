# Environment-specific ConfigMap for DEV
# Endpoints populated by bootstrap workflow
apiVersion: v1
kind: ConfigMap
metadata:
  name: vote3-config
  labels:
    app.kubernetes.io/name: vote3
    app.kubernetes.io/part-of: vote3
    environment: dev
data:
  # Redis - ElastiCache endpoint (populated by bootstrap)
  REDIS_HOST: "vote3-dev-redis.2muj25.0001.usw2.cache.amazonaws.com"
  REDIS_PORT: "6379"
  # PostgreSQL - RDS endpoint (populated by bootstrap)
  DATABASE_HOST: "vote3-dev-postgres.chwgk42cofpp.us-west-2.rds.amazonaws.com"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "votes"
  AWS_REGION: "us-west-2"
