# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  Vote Service - ClusterIP for NGINX Ingress (RULE 23: Shared Ingress)        ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

apiVersion: v1
kind: Service
metadata:
  name: vote
  labels:
    app: vote
    app.kubernetes.io/name: vote
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: vote
