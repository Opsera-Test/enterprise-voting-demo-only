# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  DEV Ingress Patch - RULE 22: Must include complete TLS section              ║
# ║  RULE 29: Uses PLACEHOLDER_SUBDOMAIN - replaced by bootstrap                 ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: voting-app-ingress
  labels:
    environment: dev
spec:
  tls:
    - hosts:
        - PLACEHOLDER_SUBDOMAIN
      secretName: voting-app-dev-tls
  rules:
    - host: PLACEHOLDER_SUBDOMAIN
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vote
                port:
                  number: 80
          - path: /result
            pathType: Prefix
            backend:
              service:
                name: result
                port:
                  number: 80
